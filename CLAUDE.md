# brick_cop

## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues on this repo (`benjaminjo1/brick_cop`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default mattpocock/skills label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` at the root (create it when domain terms emerge). See `docs/agents/domain.md`.
